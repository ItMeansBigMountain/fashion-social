import Link from "next/link";
export default async function SuccessPage({searchParams}:{searchParams:Promise<{session_id?:string}>}){const{session_id}=await searchParams;return <main className="success-page"><div className="success-mark">✓</div><p className="eyebrow">Payment received</p><h1>We’re preparing<br/><em>your order.</em></h1><p>Stripe has returned you securely. Final order status is confirmed by the signed payment event and may take a moment to appear.</p>{session_id&&<Link className="primary-button" href={`/order-status?session_id=${encodeURIComponent(session_id)}`}>View order status <span>↘</span></Link>}<Link href="/#shop">Keep exploring</Link></main>}
