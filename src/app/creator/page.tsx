import Link from"next/link";import{currentCreator}from"@/lib/creator-request";import{getCreatorDashboardDb}from"@/lib/creator-store";import CreatorConsole from"./creator-console";export const dynamic="force-dynamic";export default async function Page(){const creator=await currentCreator();if(!creator)return <main className="creator-access"><Link href="/" className="brand"><span>W</span> wornly creators</Link><p className="eyebrow">Sell through your influence</p><h1>Posts become products.</h1><p>Approved creators submit rights-cleared content, fulfill orders, earn clout, and receive Stripe payouts after delivery and the return hold. Ask the marketplace team for a one-time creator invitation.</p><Link className="primary-button" href="/support">Request creator access</Link></main>;return <CreatorConsole creator={creator} data={await getCreatorDashboardDb(creator.id) as never}/>}