import{NextResponse}from"next/server";import{cookies}from"next/headers";import{CREATOR_COOKIE,creatorCookieOptions}from"@/lib/creator-request";import{revokeCreatorSessionDb}from"@/lib/creator-store";export async function POST(){const jar=await cookies(),token=jar.get(CREATOR_COOKIE)?.value;if(token)await revokeCreatorSessionDb(token);const r=NextResponse.json({ok:true});r.cookies.set(CREATOR_COOKIE,"",{...creatorCookieOptions(process.env.NODE_ENV==="production"),maxAge:0});return r}