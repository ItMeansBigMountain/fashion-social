import{randomBytes}from"node:crypto";import{NextRequest,NextResponse}from"next/server";import{CREATOR_COOKIE,creatorCookieOptions}from"@/lib/creator-request";import{exchangeCreatorInviteDb}from"@/lib/creator-store";export async function POST(r:NextRequest){try{const{token}=await r.json();const session=randomBytes(32).toString("base64url");const creator=await exchangeCreatorInviteDb(token,session);const response=NextResponse.json({creator});response.cookies.set(CREATOR_COOKIE,session,creatorCookieOptions(process.env.NODE_ENV==="production"));return response}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Invite failed"},{status:400})}}