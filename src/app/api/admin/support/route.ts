import{NextRequest,NextResponse}from"next/server";import{currentAdmin}from"@/lib/admin-request";import{isAdminRoleAllowed}from"@/lib/admin-auth";import{updateSupportRequestDb}from"@/lib/admin-store";export async function PATCH(r:NextRequest){const a=await currentAdmin();if(!a||!isAdminRoleAllowed(a.role,"support.write"))return NextResponse.json({error:"Forbidden"},{status:403});try{const b=await r.json();if(!["open","in_progress","resolved","closed"].includes(b.status))throw new Error("Invalid support status");return NextResponse.json({request:await updateSupportRequestDb(b.id,b.status,a.email)});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Update failed"},{status:400});}}
