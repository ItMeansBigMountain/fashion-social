import {NextRequest,NextResponse} from "next/server";import {currentAdmin} from "@/lib/admin-request";import {isAdminRoleAllowed} from "@/lib/admin-auth";import {createManualShipmentDb} from "@/lib/admin-store";import {buildManualShipment} from "@/lib/shipping-service";
export async function POST(request:NextRequest){const admin=await currentAdmin();if(!admin||!isAdminRoleAllowed(admin.role,"shipment.write"))return NextResponse.json({error:"Forbidden"},{status:403});try{const shipment=buildManualShipment(await request.json());return NextResponse.json({shipment:await createManualShipmentDb(shipment,admin.email)});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Shipment failed"},{status:400});}}
