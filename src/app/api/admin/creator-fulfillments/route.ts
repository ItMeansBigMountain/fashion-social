import{NextRequest,NextResponse}from"next/server";import{currentAdmin}from"@/lib/admin-request";import{isAdminRoleAllowed}from"@/lib/admin-auth";import{markCreatorFulfillmentDeliveredDb}from"@/lib/creator-store";
export async function PATCH(r:NextRequest){const a=await currentAdmin();if(!a||!isAdminRoleAllowed(a.role,"shipment.write"))return NextResponse.json({error:"Forbidden"},{status:403});try{const b=await r.json();if(typeof b.id!=="string")throw new Error("Fulfillment ID is required");return NextResponse.json({fulfillment:await markCreatorFulfillmentDeliveredDb(b.id,a.email,b.deliveredAt)})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Delivery update failed"},{status:400})}}
