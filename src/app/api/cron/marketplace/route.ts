import{NextRequest,NextResponse}from"next/server";import{runMarketplaceAutomation}from"@/lib/creator-marketplace-store";
export async function GET(request:NextRequest){if(!process.env.CRON_SECRET)return NextResponse.json({error:"Marketplace automation is not configured"},{status:503});if(request.headers.get("authorization")!==`Bearer ${process.env.CRON_SECRET}`)return NextResponse.json({error:"Unauthorized"},{status:401});try{return NextResponse.json(await runMarketplaceAutomation())}catch(error){console.error("Marketplace automation failed",error instanceof Error?error.message:"unknown error");return NextResponse.json({error:"Marketplace automation failed"},{status:500})}}
