"use client";import{useState}from"react";export default function SupportForm(){const[status,setStatus]=useState("");return <form className="support-form" onSubmit={async e=>{e.preventDefault();setStatus("Sending…");const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/support",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const data=await r.json();setStatus(r.ok?`Request ${data.request.id} received.`:data.error)}}><label>Email<input name="email" type="email" required/></label><label>Order ID (optional)<input name="orderId" placeholder="UUID from order status"/></label><label>Category<select name="category"><option value="order">Order</option><option value="shipping">Shipping</option><option value="return">Return</option><option value="product">Product</option><option value="privacy">Privacy</option><option value="rights">Rights / takedown</option><option value="other">Other</option></select></label><label>How can we help?<textarea name="message" minLength={20} maxLength={4000} rows={8} required/></label><button className="primary-button">Submit request</button>{status&&<p role="status">{status}</p>}</form>}