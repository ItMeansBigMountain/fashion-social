const categories=["order","shipping","return","product","privacy","rights","other"] as const;export type SupportCategory=typeof categories[number];export function parseSupportRequest(input:unknown){if(!input||typeof input!=="object")throw new Error("Support request is required");const v=input as Record<string,unknown>;const email=typeof v.email==="string"?v.email.trim().toLowerCase():"";if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email))throw new Error("A valid email is required");if(typeof v.category!=="string"||!categories.includes(v.category as SupportCategory))throw new Error("Choose a valid support category");if(typeof v.message!=="string"||v.message.trim().length<20||v.message.length>4000)throw new Error("Message must be between 20 and 4,000 characters");if(v.orderId!=null&&v.orderId!==""&&(typeof v.orderId!=="string"||!/^[0-9a-f]{8}-[0-9a-f-]{27}$/i.test(v.orderId)))throw new Error("Order ID must be a UUID");return{email,category:v.category as SupportCategory,message:v.message.trim(),orderId:typeof v.orderId==="string"&&v.orderId?v.orderId:null};}
