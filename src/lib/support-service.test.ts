import{describe,expect,it}from"vitest";import{parseSupportRequest}from"./support-service";describe("support requests",()=>{it("accepts a useful request",()=>expect(parseSupportRequest({email:"buyer@example.com",category:"order",message:"Please help me locate order 12345."})).toMatchObject({email:"buyer@example.com",category:"order"}));it("rejects invalid email, category, and short spam",()=>{expect(()=>parseSupportRequest({email:"x",category:"hack",message:"hi"})).toThrow()})});
