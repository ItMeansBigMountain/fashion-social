import{parseProductMutation,type ProductMutation}from"./product-admin-service";
const uuid=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
export function parseCreatorProfile(input:unknown){if(!input||typeof input!=="object")throw new Error("Creator profile is required");const v=input as Record<string,unknown>;const email=typeof v.email==="string"?v.email.trim().toLowerCase():"";if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email))throw new Error("Valid creator email required");const handle=typeof v.handle==="string"?v.handle.replace(/^@/,"").trim().toLowerCase():"";if(!/^[a-z0-9._-]{2,50}$/.test(handle))throw new Error("Valid creator handle required");const displayName=typeof v.displayName==="string"?v.displayName.trim():"";if(displayName.length<2||displayName.length>100)throw new Error("Display name must be 2–100 characters");return{email,handle,displayName}}
export type CreatorProductSubmission={product:ProductMutation;primarySocialPostId:string;socialPostIds:string[];platformFeeBps:number};
export function parseCreatorProductSubmission(input:unknown):CreatorProductSubmission{if(!input||typeof input!=="object")throw new Error("Creator product submission is required");const v=input as Record<string,unknown>;const product=parseProductMutation(v.product);if(typeof v.primarySocialPostId!=="string"||!uuid.test(v.primarySocialPostId))throw new Error("Approved primary social post is required");if(!Array.isArray(v.socialPostIds)||!v.socialPostIds.length||v.socialPostIds.some(id=>typeof id!=="string"||!uuid.test(id)))throw new Error("At least one approved social post is required");if(!(v.socialPostIds as string[]).includes(v.primarySocialPostId))throw new Error("Primary social post must be associated with the product");const platformFeeBps=Number(v.platformFeeBps);if(!Number.isInteger(platformFeeBps)||platformFeeBps<0||platformFeeBps>10000)throw new Error("Platform fee must be 0–10,000 basis points");return{product,primarySocialPostId:v.primarySocialPostId,socialPostIds:[...new Set(v.socialPostIds as string[])],platformFeeBps}}
